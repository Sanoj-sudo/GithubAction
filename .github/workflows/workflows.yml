name: My first workflow
on: 
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
    Deploy:
      runs-on: ubuntu-latest
      steps:
         - uses: actions/checkout@v3
         - run: echo "Hello world!!"
        
    
  
